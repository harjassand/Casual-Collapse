train:
  num_steps: 300
  checkpoint_interval: 100
  log_interval: 50
  buffer_size: 500
  batch_size: 32
  horizon: 2
eval:
  steps: 30
  alignment_samples: 50
  alignment_rollouts: 5
model:
  vq_use_ema: false
  vq_soft_temp: 2.0
vq:
  entropy_reg: 0.0
